name: Deploy Pages

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    # GitHub's shortest supported scheduled-workflow interval.
    - cron: "*/5 * * * *"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v5
      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Install Chromium
        if: github.event_name != 'schedule'
        run: npx playwright install --with-deps chromium
      - name: Generate automatic LKML catalog
        env:
          LOREFOLD_MAX_LISTS: "8"
          LOREFOLD_MAX_THREADS_PER_LIST: "4"
        run: timeout --signal=TERM 120s npm run data:generate || echo "::warning::upstream catalog sync timed out; deploying the last valid catalog"
      - name: Test Pages-base production artifact
        if: github.event_name != 'schedule'
        env:
          VITE_BASE_PATH: /Lorefold/
          E2E_BASE_URL: http://127.0.0.1:4173/Lorefold/
        run: npm run test:e2e
      - name: Build Pages artifact
        env:
          VITE_BASE_PATH: /Lorefold/
        run: npm run build
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
