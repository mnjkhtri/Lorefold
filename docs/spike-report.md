# spike report

the real pages-origin browser probe failed to read lore responses. preflight
failed too. direct loading is off.

github actions can fetch with a descriptive user agent, so the automatic path
generates a bounded same-origin catalog from Lore's discovered archive index.
