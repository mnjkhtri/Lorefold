# spike report

the real pages-origin browser probe failed to read lore responses. preflight
failed too. direct loading is off.

the local mbox path works and remains the mvp path. a future direct mode needs
a successful browser cors test first.
