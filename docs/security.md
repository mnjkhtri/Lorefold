# security

mail is hostile text.

no html injection. no remote images. no trackers. no arbitrary attachments.
raw mail stays in a `pre`. the page has a self-only csp. lore links are strict.
parsing has byte, gzip, mime, header, record, reference, and depth limits.

no proxy. no api. no `no-cors` tricks.
